let initialState={
    status:'change',
    editMode: false
}
let StatusReducer=(state = initialState, action:any)=>{
    switch (action.type){
        default:
            return state
    }
}
export default StatusReducer;